# Rust build output
/target
/adapters/*/target
/adapters/*/Cargo.lock

# Runtime state
/data
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Editor / OS
.idea/
.vscode/
*.swp
Thumbs.db
Desktop.ini

# Cargo.lock remains committed so CI and embedded consumers exercise one exact
# dependency graph; library users still resolve dependencies normally.
